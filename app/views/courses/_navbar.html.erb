<% if course.id != nil%>
    <br>
    <strong>
      <%= link_to course, course %>:
    </strong>
    <ul class="nav nav-tabs">
      <li><%= link_to 'Assignments', course_assignments_path(course_id: course.id) %></li>
      <li><%= link_to 'Students', students_course_path(id: course.id) %></li>
    </ul>
<% end %>
